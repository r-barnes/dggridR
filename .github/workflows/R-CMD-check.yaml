# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
on:
  push:
    branches: [main, master]
  pull_request:

name: R-CMD-check.yaml

permissions: read-all

jobs:
  R-CMD-check:
    runs-on: ${{ matrix.config.os }}

    name: ${{ matrix.config.os }} (${{ matrix.config.r }})

    strategy:
      fail-fast: false
      matrix:
        config:
          - {os: macos-latest,   r: 'release'}
          - {os: windows-latest, r: 'release'}
          - {os: ubuntu-latest,   r: 'devel', http-user-agent: 'release'}
          - {os: ubuntu-latest,   r: 'release'}
          - {os: ubuntu-latest,   r: 'oldrel-1'}

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}
          http-user-agent: ${{ matrix.config.http-user-agent }}
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::rcmdcheck
          needs: check

      - uses: r-lib/actions/check-r-package@v2
        with:
          upload-snapshots: true
          build_args: 'c("--no-manual","--compact-vignettes=gs+qpdf")'

  debian-clang-check:
    runs-on: ubuntu-latest
    container:
      image: debian:testing

    name: Debian testing (dggridR with clang)

    steps:
      - name: Install system dependencies
        run: |
          apt-get update -y
          apt-get install -y --no-install-recommends \
            ca-certificates \
            clang \
            libc++-dev \
            libc++abi-dev \
            git \
            r-base-dev \
            libgdal-dev \
            libgeos-dev \
            libgeos++-dev \
            libproj-dev \
            libudunits2-dev \
            libssl-dev \
            libcurl4-openssl-dev \
            libxml2-dev \
            libfontconfig1-dev \
            libharfbuzz-dev \
            libfribidi-dev \
            libfreetype6-dev \
            libpng-dev \
            libtiff5-dev \
            libjpeg-dev \
            libsqlite3-dev \
            libbz2-dev \
            zlib1g-dev \
            libuv1-dev \
            libgit2-dev \
            pandoc \
            qpdf \
            ghostscript \
            wget \
            cmake \
            pkg-config

      - uses: actions/checkout@v4

      - name: Configure GCC for dependency installation
        run: |
          # Display available compilers
          echo "=== Available Compilers ==="
          gcc --version
          g++ --version
          clang --version
          clang++ --version

          # Explicitly set GCC as compiler for R package installation
          mkdir -p ~/.R
          cat > ~/.R/Makevars <<EOF
          CC=gcc
          CXX=g++
          CXX11=g++
          CXX14=g++
          CXX17=g++
          CXX20=g++
          EOF
          echo "=== GCC configured in ~/.R/Makevars ==="
          cat ~/.R/Makevars

          # Configure spatial libraries
          echo "=== Spatial Library Configuration ==="
          gdal-config --version
          geos-config --version

      - name: Install R package dependencies with GCC
        env:
          CC: gcc
          CXX: g++
        run: |
          # Use GCC for dependencies (s2/sf have issues with Clang 21)
          options(repos = c(CRAN = 'https://cloud.r-project.org'))

          cat("Current compiler settings:\n")
          cat("CC:", Sys.getenv("CC"), "\n")
          cat("CXX:", Sys.getenv("CXX"), "\n")
          cat("Makevars:\n")
          cat(readLines("~/.R/Makevars"), sep = "\n")

          cat("\nInstalling remotes and rcmdcheck...\n")
          install.packages(c('remotes', 'rcmdcheck'), Ncpus = 2)

          cat("\nInstalling required dependencies with GCC...\n")
          cat("(Dependencies compiled with GCC, dggridR will use Clang)\n")
          # Install only required dependencies, skip suggested packages
          remotes::install_deps(dependencies = NA, upgrade = "never", Ncpus = 2)

          cat("\nVerifying required packages:\n")
          required_pkgs <- c("Rcpp", "collapse", "sf", "wk", "rcmdcheck")
          all_ok <- TRUE
          for (pkg in required_pkgs) {
            if (requireNamespace(pkg, quietly = TRUE)) {
              cat(sprintf("✓ %s: %s\n", pkg, packageVersion(pkg)))
            } else {
              cat(sprintf("✗ %s: NOT INSTALLED\n", pkg))
              all_ok <- FALSE
            }
          }
          if (!all_ok) stop("Required packages failed to install")
          cat("\n✓ All required dependencies installed successfully with GCC\n")
        shell: Rscript {0}

      - name: Configure Clang for dggridR compilation
        run: |
          # Now reconfigure to use Clang for dggridR package compilation
          cat > ~/.R/Makevars <<EOF
          CC=clang
          CXX=clang++
          CXX11=clang++
          CXX14=clang++
          CXX17=clang++
          CXX20=clang++
          EOF

          echo "CC=clang" >> $GITHUB_ENV
          echo "CXX=clang++" >> $GITHUB_ENV

          echo "=== Clang configured for dggridR compilation ==="
          cat ~/.R/Makevars

      - name: Show R and compiler configuration for dggridR
        run: |
          cat("R version:\n")
          print(R.version)
          cat("\nCompiler configuration for dggridR:\n")
          cat("CC:", Sys.getenv("CC"), "\n")
          cat("CXX:", Sys.getenv("CXX"), "\n")
          system("clang --version")
          cat("\nMakevars configuration:\n")
          if (file.exists("~/.R/Makevars")) {
            cat(readLines("~/.R/Makevars"), sep = "\n")
          } else {
            cat("No ~/.R/Makevars file\n")
          }
        shell: Rscript {0}

      - name: Check package
        run: |
          Sys.setenv("_R_CHECK_FORCE_SUGGESTS_" = "false")
          rcmdcheck::rcmdcheck(
            args = c("--no-manual", "--as-cran", "--no-vignettes"),
            build_args = c("--no-manual", "--no-build-vignettes"),
            error_on = "warning",
            check_dir = "check"
          )
        shell: Rscript {0}

      - name: Upload check results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: debian-clang-results
          path: check
